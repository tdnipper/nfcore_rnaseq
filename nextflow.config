process {
  executor = 'local'
  cpus = 2
  memory = '4 GB'
}

workDir = './work'

// By default we run processes locally. If you'd like to use a different executor adjust here.
// If you want Nextflow to run containers directly, update process.container and related settings.
